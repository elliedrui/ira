import React from 'react'

const Members = (props) => {
  console.log('in the member', props)
  return (
    <div>
      <h2>Members</h2>
      {/* {props.members.map(member => 
      <li key={member.id}> 
          {member.attributes.name} - {member.attributes.email} - {member.attributes.admin_level}
        </li>
      )} */}
      mermber mremerne
    </div>
  )
}

export default Members